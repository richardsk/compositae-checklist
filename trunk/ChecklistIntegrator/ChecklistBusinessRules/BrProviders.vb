Imports ChecklistObjects
Imports ChecklistDataAccess

Public Class BrProviders

    'Public Shared Function GetAutoProviderImport() As ProviderImport
    '    Dim pi As ChecklistObjects.ProviderImport

    '    Try
    '        Dim ps As Provider() = ProviderData.GetProviders()
    '        For Each p As Provider In ps
    '            If p.Name = "SYSTEM" Then
    '                Dim pis As ProviderImport() = ImportData.GetProviderImports(p.IdAsInt)
    '                If pis.Length > 0 Then pi = pis(0)
    '            End If
    '        Next
    '    Catch ex As Exception
    '    End Try

    '    Return pi
    'End Function

End Class
